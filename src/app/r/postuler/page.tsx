import { ReworkPlaceholder } from "@/components/ReworkPlaceholder";

export const metadata = { title: "Postuler — R2JC Reworked" };

export default function ReworkedPostuler() {
  return <ReworkPlaceholder index="[02]" section="Postuler" upgradedHref="/postuler" />;
}
