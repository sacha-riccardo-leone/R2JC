import { ReworkPlaceholder } from "@/components/ReworkPlaceholder";

export const metadata = { title: "Éditions — R2JC Reworked" };

export default function ReworkedEditions() {
  return <ReworkPlaceholder index="[01]" section="Éditions" upgradedHref="/editions" />;
}
